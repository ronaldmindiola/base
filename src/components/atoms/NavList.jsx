import React from "react";
/* import { Link } from "react-router-dom";
import {
  Box,
  Button
} from "@mui/material"; */
//import config from "../../config.json";

const NavList = () => {
  //const { pages } = config;
  return (
    <>
      {/* <Box sx={{ flexGrow: 1, display: { xs: "none", sm: "flex" }, spacing:2 }}>
        {pages.map((page) => (
          <Button
            underline="hover"
            key={page.label}
            to={page.path}
            sx={{
              color: "gainsboro",
              my: 0,
              display: "block",
              textTransform: "capitalize",
              fontSize: 16,
              '&:hover': {
                backgroundColor: '#000000'
              }
            }}
          >
            {page.label}
          </Button>
        ))}
      </Box> */}11
      
    </>
  );
};

export default NavList;
